import {Routes} from '@angular/router';

import {MarketAccountComponent} from '../market-account.component';

export const marketAccountRoutes: Routes = [
    {
        path: '',
        component: MarketAccountComponent,
    },
];
