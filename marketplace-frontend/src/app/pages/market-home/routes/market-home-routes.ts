import {Routes} from '@angular/router';

import {MarketHomeComponent} from '../market-home.component';

export const marketHomeRoutes: Routes = [
    {
        path: '',
        component: MarketHomeComponent,
    },
];
